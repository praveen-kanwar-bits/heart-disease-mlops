{
  "title": "Heart Disease API",
  "panels": [
    {
      "type": "timeseries",
      "title": "Prediction Request Rate",
      "targets": [{"expr": "rate(heart_predict_requests_total[5m])"}]
    },
    {
      "type": "timeseries",
      "title": "Prediction Latency p95",
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(heart_predict_latency_seconds_bucket[5m])) by (le))"}]
    }
  ],
  "schemaVersion": 39,
  "version": 1
}
